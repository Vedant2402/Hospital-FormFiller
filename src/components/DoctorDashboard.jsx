// Deprecated: DoctorDashboard is removed in the patient-only flow.
// Keeping a tiny placeholder to avoid accidental imports breaking the build during transition.
export default function DoctorDashboard() {
  return null;
}